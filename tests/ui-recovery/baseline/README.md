# UI recovery baseline

`home-before.png` is the 1280×900 production home reference captured from commit `a6c73fc` with a deterministic local session (`North Workshop`, `Release Auditor`). It records the repeated page-header/flat-navigation state before the shared workspace shell.
