# Reviewed UI recovery images

`home-1280.png` is the deterministic desktop Home checkpoint for UIR-03. It was inspected after legacy demo CSS was moved behind the lazy `/demo` boundary and the production token layer was applied.
