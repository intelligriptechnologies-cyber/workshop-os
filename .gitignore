node_modules/
dist/
test-results/
playwright-report/
.env
.env.*
*.log

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
.worktrees/

# tool-session artifacts, not project files
/.claude/
/.playwright-mcp/
/.serena/
